/*
package edu.gla.kail.ad.core;

import edu.gla.kail.ad.Client.InputInteraction;
import edu.gla.kail.ad.Client.InteractionType;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.junit.runners.JUnit4;

import static org.junit.Assert.assertTrue;

@RunWith(JUnit4.class)
public class DummyAgentTest {
    */
/**
     * Test if DummyAgent is returning response.
     *//*

    @Test
    public void testDummyAgentResponse() {
        InputInteraction inputInteraction = InputInteraction.newBuilder()
                .setType(InteractionType.TEXT)
                .setText("Sample text")
                .setDeviceType("iPhone whatever")
                .setLanguageCode("en-US")
                .build();
        DummyAgent dummyAgent = new DummyAgent();
        assertTrue("DummyAgent's response is not initialized!", dummyAgent.getResponseFromAgent
                (inputInteraction).isInitialized());
    }
}
*/
